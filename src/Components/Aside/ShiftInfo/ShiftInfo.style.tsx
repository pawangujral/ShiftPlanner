import { styled } from '@mui/system';
import { grey } from '@mui/material/colors';

export const TotalTime = styled('span')({
  fontSize: '10px',
  color: grey[600],
  textAlign: 'left',
  display: 'block',
});
